var chai = require('chai');
var chaiHttp = require('chai-http');
var server = require('../app');
var expect = chai.expect;

chai.use(chaiHttp);


describe('signup controller', function() {
  it('should list the questions for sign up on /signup GET');
});